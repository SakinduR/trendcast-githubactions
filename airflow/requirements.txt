# Additional Python packages for the Airflow container.
# The base Airflow image already includes apache-airflow; only add extras here.
# Installed during Docker build via: pip install -r requirements.txt

apache-airflow-providers-postgres==5.13.1
requests==2.32.3
psycopg2-binary==2.9.10
